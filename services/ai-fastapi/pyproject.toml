[tool.poetry]
name = "ai-fastapi"
version = "0.1.0"
description = "FastAPI Intelligence Service"
authors = ["AI Team <ai@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.100.0"
uvicorn = "^0.23.0"
pydantic = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
